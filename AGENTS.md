# 에이전트 개발 가이드라인 (AGENTS.md)

이 문서는 이 리포지토리에서 작업하는 모든 AI 에이전트가 준수해야 하는 개발 규칙, 환경 설정 및 행동 지침을 정의합니다. 에이전트는 작업을 시작하기 전에 이 가이드를 반드시 숙지해야 합니다.

---

## 1. 언어 규칙 (Language Guidelines)

- **문서화**: 모든 마크다운 문서(README.md, AGENTS.md 및 신규 문서)는 **한국어**로 작성합니다.
- **주석 및 출력**: 소스 코드 내의 주석(Comments)과 로그/출력 메시지(`print()` 등)는 **한국어**로 작성합니다.
- **코드 식별자**: 변수명, 함수명, 클래스명 등의 코드 식별자는 영어 단어를 사용하여 작성합니다.

---

## 2. 커밋 메시지 규칙 (Commit Message Guidelines)

- **기본 포맷**: Conventional Commits 규격을 따릅니다.
- **언어**: 접두어(Type)는 영문으로 유지하고, 본문 설명은 **한국어**로 작성합니다.
- **형식**: `<type>: <description>`
  - 예시: `feat: LED 제어 기능 추가`
  - 예시: `docs: AGENTS.md 가이드라인 파일 추가`
- **주요 Type**:
  - `feat`: 새로운 기능 추가
  - `fix`: 버그 수정
  - `docs`: 문서 수정 및 추가
  - `style`: 코드 포맷팅, 세미콜론 누락 등 (코드 변경 없음)
  - `refactor`: 코드 리팩토링 (기능 변화 없음)
  - `chore`: 빌드 업무 수정, 패키지 매니저 설정 등

---

## 3. 파이썬 및 패키지 관리 규칙 (Python & uv Guidelines)

- **도구 규정**: 파이썬 개발 환경 및 의존성 관리는 반드시 **`uv`**를 사용합니다. `pip` 사용은 전면 금지합니다.
- **패키지 추가/제거**: 
  - 패키지 설치: `uv add <package_name>`
  - 패키지 제거: `uv remove <package_name>`
- **스크립트 실행**: 파이썬 파일 실행 시 가상환경 진입 유무와 상관없이 반드시 `uv run python <script_name>` 명령을 활용합니다.
  - 예시: `uv run python main.py`

---

## 4. 검증 및 행동 지침 (Verification & Behavior Guidelines)

- **코드 포맷팅**: 별도의 자동 포맷터(Black, Ruff 등)를 강제 적용하지 않습니다. 기존 코드의 스타일(들여쓰기, 네이밍 등)을 존중하여 일관성 있게 작성합니다.
- **하드웨어 제어 검증**: 
  - 햄스터 로봇(`roboid` 라이브러리)의 동작을 제어하는 코드는 에이전트가 직접 물리적으로 실행하여 검증하기 어렵습니다.
  - 에이전트는 코드에 구문 오류(Syntax Error)나 임포트 에러가 없는지 정적 분석 위주로 검증을 진행합니다.
  - 로봇의 실제 이동, LED 발광 등 물리적 동작 확인은 사용자(USER)에게 **수동 검증을 정중히 요청**합니다.
